# KeenAI v0.2.0 (planned)

**Gate**: complete **all** features in [08-ROADMAP.md](../08-ROADMAP.md) Phase 0, 1, 2, and 3 before tagging.  
Track: [08-ROADMAP-TODO.md](../08-ROADMAP-TODO.md) § v0.2.0 发布门禁 · [GA.md](../GA.md).
Architecture and module status: [V0.2.0-ARCHITECTURE-MODULE-STATUS.md](../V0.2.0-ARCHITECTURE-MODULE-STATUS.md).

## Scope summary

| Phase | Must ship |
|-------|-----------|
| 0 | Monorepo · CI/CD · Auth · Fumadocs · CLI · OTel/pino |
| 1 | Widget · Email · Inbox · Copilot · Workflow MVP · Alpha 验收 |
| 2 | Tickets · Workflow 完整 · Multimodal · Feedback · Help Center · 渠道 · 分析 |
| 3 | Agent · Memory · KB 生产深度 · CA/MCP 产品面 · 国产化 · Sprint 18 收尾 |

## Release checklist (I119～I120)

- [ ] Phase 0～3 清单全 `[x]`
- [ ] Recall@5 ≥ 88% · stale-answer proxy < 2% via `kb:release-gate` · CI green · local P95 via `kb:bench:local` and `kb:bench:api:local` · deployed API `kb:bench` documented
- [ ] Docker images `0.2.0` on GHCR via `Docker Publish` workflow
- [ ] `pnpm release:verify-external` passes against `artifacts/release/v0.2.0-external-evidence.json`
- [ ] `CHANGELOG [0.2.0]` · tag `v0.2.0` · GitHub Release (prerelease)

## Release candidate notes

### Added

- Qwen and Zhipu provider support for localized Chinese AI responses.
- Dashboard next-intl registration for 12 locales.
- Intercom users/admins plus conversations/messages import.
- KB `kb:release-gate`, `kb:bench:local`, and API-level `kb:bench:api:local` release checks.
- KB query-log telemetry report builder for production stale-answer proxy, latency, and insufficient-data release evidence.
- Authenticated KB telemetry API and `pnpm kb:telemetry` JSON/Markdown export for release evidence attachment.
- External evidence manifest template and `pnpm release:verify-external` pre-tag validator.
- Config-backed `file`, shallow `web`, GitHub raw content, and Notion page/block KB source connectors.
- Signed KB source webhook entrypoint for GitHub and Notion events to trigger KB-16 ingest.
- GHCR Docker publish workflow for API and Dashboard images.
- Helm `0.2.0` defaults and CI lint/template validation.
- Internal support flow JSON/Markdown evidence via `pnpm support:flow:report`, embedded into `release:evidence`.
- Dashboard dogfood JSON/Markdown evidence via `pnpm support:dogfood:report`, embedded into `release:evidence`.
- Customer reachability JSON/Markdown evidence via `pnpm customer:reachability:report`, embedded into `release:evidence`.
- Widget runtime JSON/Markdown evidence via `pnpm widget:runtime:report`, embedded into `release:evidence`.
- Auto resolution JSON/Markdown evidence via `pnpm auto:resolution:report`, embedded into `release:evidence`.
- Featurebase parity JSON/Markdown evidence via `pnpm featurebase:parity:report`, embedded into `release:evidence`.
- Core module parity JSON/Markdown evidence via `pnpm core:parity:report`, embedded into `release:evidence` for frontend, RAG, memory, storage, multimodal, agent, and workflow design-doc coverage.
- Phase 0-3 gate JSON/Markdown evidence via `pnpm phase:gate:report`, embedded into `release:evidence` for all 70 must-complete checklist items.
- Docs/tutorial release JSON/Markdown evidence via `pnpm docs:release:report`, embedded into `release:evidence`.
- Quality gate JSON/Markdown evidence via `pnpm quality:gate:report`, embedded into `release:evidence`.
- Copilot adoption JSON/Markdown evidence via `pnpm copilot:adoption:report`, embedded into `release:evidence`.
- Docker lite startup JSON/Markdown evidence via `pnpm docker:lite:startup-report`, embedded into `release:evidence`.
- Tutorial video scripts and publishing checklist under `docs/tutorials/v0.2.0`.
- Ollama offline demo evidence via `pnpm ollama:offline:demo`, embedded into `release:evidence`.
- KB eval JSON/Markdown release evidence via `pnpm kb:eval:report`, embedded into `release:evidence`.
- v0.2.0 architecture, app/package inventory, module status, and remaining external evidence handoff.

### Deepened

- KB-16 ingest now runs connector sync, parse/clean/chunk/enrich/embed/index/notify, source status/error updates, and Inngest retries.
- KB source dispatch now supports both conversation crystallization and source ingest events.
- KB-18 parser/chunker, KB-19 crystallization gates, KB-20 contradiction checks, and KB-22 context reranking now have local test coverage.
- Root release typecheck uses `corepack pnpm -r --if-present typecheck` to avoid pnpm version drift.

### Evidence captured

- `CI=true corepack pnpm lint`
- `CI=true corepack pnpm typecheck`
- `CI=true corepack pnpm test` (192 files / 420 tests at capture time)
- `CI=true corepack pnpm kb:eval`
- `CI=true corepack pnpm support:flow:report`
- `CI=true corepack pnpm support:dogfood:report`
- `CI=true corepack pnpm customer:reachability:report`
- `CI=true corepack pnpm widget:runtime:report`
- `CI=true corepack pnpm auto:resolution:report --fixture`
- `CI=true corepack pnpm featurebase:parity:report`
- `CI=true corepack pnpm core:parity:report`
- `CI=true corepack pnpm phase:gate:report`
- `CI=true corepack pnpm docs:release:report`
- `CI=true corepack pnpm test:coverage` (195 files / 440 tests; lines/statements 82.76%, functions 87.28%)
- `CI=true corepack pnpm quality:gate:report`
- `CI=true corepack pnpm copilot:adoption:report --fixture`
- `CI=true DOCKER_LITE_STARTUP_DRY_RUN=true corepack pnpm docker:lite:startup-report`
- `CI=true corepack pnpm ollama:offline:demo`
- `CI=true corepack pnpm kb:eval:report`
- `CI=true SUPPORT_FLOW_REPORT_JSON=artifacts/release/support-flow.json SUPPORT_DOGFOOD_REPORT_JSON=artifacts/release/support-dogfood.json CUSTOMER_REACHABILITY_REPORT_JSON=artifacts/release/customer-reachability.json WIDGET_RUNTIME_REPORT_JSON=artifacts/release/widget-runtime.json AUTO_RESOLUTION_REPORT_JSON=artifacts/release/auto-resolution.json FEATUREBASE_PARITY_REPORT_JSON=artifacts/release/featurebase-parity.json CORE_MODULE_PARITY_REPORT_JSON=artifacts/release/core-module-parity.json PHASE_GATE_STATUS_REPORT_JSON=artifacts/release/phase-gate-status.json DOCS_RELEASE_REPORT_JSON=artifacts/release/docs-release.json QUALITY_GATE_REPORT_JSON=artifacts/release/quality-gate.json COPILOT_ADOPTION_REPORT_JSON=artifacts/release/copilot-adoption.json DOCKER_LITE_STARTUP_REPORT_JSON=artifacts/release/docker-lite-startup.json OLLAMA_OFFLINE_REPORT_JSON=artifacts/release/ollama-offline-demo.json KB_EVAL_REPORT_JSON=artifacts/release/kb-eval-report.json RELEASE_EVIDENCE_DIR=/private/tmp/keenai-release-evidence corepack pnpm release:evidence`
- `CI=true corepack pnpm exec vitest run packages/kb/src/eval/telemetry-report.test.ts`
- `CI=true DATABASE_URL=:memory: KB_TELEMETRY_MIGRATE=true KB_TELEMETRY_ORG_ID=org_local KB_TELEMETRY_JSON=/private/tmp/keenai-telemetry-cli/report.json KB_TELEMETRY_MD=/private/tmp/keenai-telemetry-cli/report.md corepack pnpm kb:telemetry`
- `CI=true corepack pnpm exec vitest run packages/kb/src/source-webhook.test.ts apps/api/src/kb.integration.test.ts`
- `CI=true corepack pnpm kb:bench:local` (`billing` p95 10.1ms, `refund policy` p95 7.1ms, errors 0)
- `CI=true KB_API_BENCH_DURATION=1 corepack pnpm kb:bench:api:local` (`billing` p95 24.3ms, `refund policy` p95 18.6ms, errors 0)
- `CI=true corepack pnpm release:evidence`
- `corepack pnpm release:verify-external /private/tmp/keenai-external-evidence/manifest.json`
- `helm lint deploy/helm/keenai`
- `helm template keenai deploy/helm/keenai`

### Remaining blockers

- P3-12 rendered tutorial MP4s or YouTube/Bilibili publish links.
- Running-environment dogfood/UI artifact for internal support end-to-end.
- Running-environment widget embed plus SMTP/IMAP or webhook ingest evidence for customer reachability.
- Production/prod-like auto resolution report with rate ≥ 50%.
- Manual screenshot/UX parity artifact for Featurebase 90% final acceptance.
- Production/prod-like Copilot adoption report with accept rate ≥ 30%.
- Actual Docker lite startup report with `evidenceStatus: "pass"` from a Docker-enabled release environment.
- Real `ollama serve` + local model runtime evidence for P3-ACC-02 final acceptance.
- Production/prod-like faithfulness, Recall@5, and stale-answer telemetry data or attached `KB_TELEMETRY_REPORT_JSON` from live/prod-like query logs.
- Live OAuth App credential proof for GitHub/Notion source setup.
- Remote CI run history and deployed-service `pnpm kb:bench` P95.
- Downloadable `v0.2.0-release-evidence` artifact from a successful remote CI run.
- Actual GHCR `0.2.0` image publish and Helm install artifact.
- Passing `release:verify-external` manifest assembled from real external artifacts.
- External KPI evidence: adoption, teams, parity, startup timing, and live ops metrics.
