# KeenAI v0.1.0

First **0.1.x** release — Alpha+ with KB GA prep, CSAT, import, and Dashboard stubs. Not a 1.0 GA; API may still change.

## Highlights

- **KB lifecycle**: conversation close → crystallize; `auto_index` FTS; golden eval (`pnpm kb:eval`) + nightly CI
- **CSAT**: conversation `rating` on agent PATCH + widget endpoint; crystallize only with explicit CSAT ≥ 4
- **Import**: `keenai import zendesk --kb` → `kb_documents`
- **Dashboard**: Custom Actions 4-step wizard stub; Help Center KB search page
- **Docs**: [DESIGN-CODE-AUDIT.md](../DESIGN-CODE-AUDIT.md), [DEPLOYMENT.md](../DEPLOYMENT.md), [GA.md](../GA.md) (0.1.0 checklist)

## Upgrade from v0.0.1 (Alpha)

```bash
git pull
pnpm install
pnpm db:migrate
```

Optional env for KB-19 LLM crystallize:

```bash
OPENAI_API_KEY=sk-...
KEENAI_CRYSTALLIZE_MODEL=gpt-4o-mini
```

## Release checklist

See [GA.md](../GA.md). Tag: `v0.1.0`.

```bash
git tag v0.1.0
git push origin v0.1.0
```

GitHub Actions [release.yml](../../.github/workflows/release.yml) publishes notes from this file.

## Known limitations

- Intercom import still plan-only; Zendesk tickets not imported
- Helm chart planned only (`deploy/helm/README.md`)
- Quality gates (Recall@5 ≥ 92%, etc.) not yet certified on production golden set
